# ~/.cognilayer/config.yaml
# CogniLayer v3 — FTS5 + vector search, 13 MCP tools

# General
version: "3.1.0"
language: "en"  # "en" or "cs"

# Paths (auto-detected)
paths:
  home: "~/.cognilayer"
  db: "~/.cognilayer/memory.db"
  log: "~/.cognilayer/logs/cognilayer.log"

# File Indexer
indexer:
  scan_depth: 3
  chunk_max_chars: 2000
  chunk_overlap_chars: 200
  doc_extensions:
    - ".md"
    - ".txt"
    - ".json"
    - ".yaml"
    - ".yml"
    - ".toml"
  code_extensions:
    - ".ts"
    - ".tsx"
    - ".js"
    - ".jsx"
    - ".py"
    - ".php"
    - ".css"
    - ".html"
  doc_max_file_size: 200000
  code_max_file_size: 50000
  ignore_dirs:
    - "node_modules"
    - ".git"
    - "__pycache__"
    - ".next"
    - "dist"
    - "build"
    - "venv"
    - ".venv"
  ignore_files:
    - "package-lock.json"
    - "yarn.lock"
    - "pnpm-lock.yaml"

# Search (FTS5 + optional vector via fastembed/sqlite-vec)
search:
  default_limit: 5
  max_limit: 10

# Project DNA
dna:
  max_tokens: 150
  template: |
    ## Project DNA: {name}
    Stack: {stack}
    Style: {style}
    Structure: {structure}
    Deploy: {deploy}
    Active: {active}
    Patterns: {patterns}
    Last: {last}

# Session
session:
  bridge_max_tokens: 150

# Projects — set to your projects directory
projects:
  base_path: "~/projects"

# Logging
logging:
  level: "INFO"
  max_size_mb: 10
